[package]
name = "thetacrypt_proto"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.8"
prost = "0.11"
serde = { version = "1.0", features = ["derive"] }

[build-dependencies]
tonic-build = "0.8"
prost-build = "0.11.1"